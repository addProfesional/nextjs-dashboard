function LayoutInvoices ({ children } : { children: React.ReactNode }) {
  return (
    <section>
      <h1>Esto es el layout de Invoice</h1>
      {children}
    </section>
  )
}

export default LayoutInvoices;